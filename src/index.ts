export * from './theme';
export * from './components';

// Re-export MUI components for convenience if needed, 
// or let the user import them directly from @mui/material.
// Most libraries prefer the latter to avoid bundling overhead,
// but for a "tuned" library, you might want to wrap them.
